import { MonthByWeekPipe } from './month-by-week.pipe';

describe('MonthByWeekPipe', () => {
  it('create an instance', () => {
    const pipe = new MonthByWeekPipe();
    expect(pipe).toBeTruthy();
  });
});
